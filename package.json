{
  "name": "codechef-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && yarn build && yarn export && touch out/.nojekyll && gh-pages -d out -t true"
  },
  "dependencies": {
    "next": "10.0.9",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-icons": "^3.11.0",
    "react-lottie": "^1.2.3",
    "styled-components": "^5.2.1",
    "yarn": "^1.22.10"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.12.0",
    "gh-pages": "^3.1.0"
  }
}
